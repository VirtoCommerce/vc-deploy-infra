apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: ceph-cert
spec:
  secretName: ceph-tls
  issuerRef:
    name: ceph-issuer
    kind: Issuer
  commonName: ceph.virtoway.us
  dnsNames:
  - ceph.virtoway.us